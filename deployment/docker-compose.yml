services:
  ms-loan-applications:
    build:
      context: ..                 
      dockerfile: deployment/Dockerfile
    env_file:
      - .env
    ports:
      - "${APP_PORT}:8081"
    networks:
      - crediya-net

networks:
  crediya-net:
    driver: bridge
    external: true

